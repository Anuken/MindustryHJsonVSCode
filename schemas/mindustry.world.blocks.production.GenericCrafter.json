{
  "superclass": "mindustry.world.Block",
  "outputItem":{
    "type": "mindustry.type.ItemStack",
    "nullable": true,
    "doc": "Written to outputItems as a single-element array if outputItems is null."
  },
  "outputItems":{
    "type": "mindustry.type.ItemStack[]",
    "nullable": true,
    "doc": "Overwrites outputItem if not null."
  },
  "outputLiquid":{
    "type": "mindustry.type.LiquidStack",
    "nullable": true,
    "doc": "Written to outputLiquids as a single-element array if outputLiquids is null."
  },
  "outputLiquids":{
    "type": "mindustry.type.LiquidStack[]",
    "nullable": true,
    "doc": "Overwrites outputLiquid if not null."
  },
  "liquidOutputDirections":{
    "type": "int[]",
    "doc": "Liquid output directions, specified in the same order as outputLiquids. Use -1 to dump in every direction. Rotations are relative to block.",
    "default": "{ -1 }"
  },
  "dumpExtraLiquid":{
    "type": "boolean",
    "doc": "if true, crafters with multiple liquid outputs will dump excess when there's still space for at least one liquid type",
    "default": "true"
  },
  "ignoreLiquidFullness":{
    "type": "boolean",
    "default": "false"
  },
  "craftTime":{
    "type": "float",
    "default": "80"
  },
  "craftEffect":{
    "type": "mindustry.entities.Effect",
    "default": "none"
  },
  "updateEffect":{
    "type": "mindustry.entities.Effect",
    "default": "none"
  },
  "updateEffectChance":{
    "type": "float",
    "default": "04"
  },
  "updateEffectSpread":{
    "type": "float",
    "default": "4"
  },
  "warmupSpeed":{
    "type": "float",
    "default": "019"
  },
  "legacyReadWarmup":{
    "type": "boolean",
    "doc": "Only used for legacy cultivator blocks.",
    "default": "false"
  },
  "drawer":{
    "type": "mindustry.world.draw.DrawBlock"
  }
}