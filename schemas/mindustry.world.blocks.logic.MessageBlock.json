{
  "superclass": "mindustry.world.Block",
  "maxTextLength":{
    "type": "int",
    "doc": "writeUTF maximum UTF-8 length per char is 3, so 3*400 = 1200 bytes, the max string byte size",
    "default": "400"
  },
  "maxNewlines":{
    "type": "int",
    "default": "24"
  }
}